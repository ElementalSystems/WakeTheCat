{
  "name": "wakethecat",
  "version": "1.0.0",
  "description": "A JS13kgames 2025 WEBVR three.js Entry",
  "main": "index.js",
  "scripts": {
    "serve": "live-server . --host=localhost --port=8080",
    "compress": "html-minifier-terser index.html -o dist/index.html  --collapse-whitespace --remove-comments --remove-redundant-attributes --remove-empty-attributes --remove-optional-tags --minify-js true --minify-css true&&terser js/levels.js -o dist/js/levels.js --compress --mangle --module&&terser js/mouse.js -o dist/js/mouse.js --compress --mangle --module&&terser js/objects.js -o dist/js/objects.js --compress --mangle --module&&terser js/textures.js -o dist/js/textures.js --compress --mangle --module&&terser js/util.js -o dist/js/util.js --compress --mangle --module&&terser js/world.js -o dist/js/world.js --compress --mangle --module&&terser js/vr.js -o dist/js/vr.js --compress --mangle --module&&terser js/sounds.js -o dist/js/sounds.js --compress --mangle --module&&cd dist&&bestzip ../release/final.zip *"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ElementalSystems/WakeTheCat.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ElementalSystems/WakeTheCat/issues"
  },
  "homepage": "https://github.com/ElementalSystems/WakeTheCat#readme",
  "devDependencies": {
    "bestzip": "^2.2.1",
    "html-minifier-terser": "^7.2.0",
    "http-server": "^14.1.1",
    "live-server": "^1.2.2",
    "terser": "^5.44.0"
  }
}